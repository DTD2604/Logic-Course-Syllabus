package Ham;

// + Tham số đầu vào của hàm.
// + Giá trị trả về của hàm.
// + Từ khóa return.

public class Bai_2{
    
    // 1. Viết một hàm sum nhận vào hai tham số kiểu int và trả về tổng của chúng.

    public static int sum(int a, int b) {
        return a + b;
    }

    // 2. Viết một hàm multiply nhận vào hai tham số kiểu double và trả về tích của chúng.

    public static double multiply(double x, double y) {
        return x * y;
    }

    // Bài Tập Về Nhà:

// 1. Viết một hàm subtract nhận vào hai tham số kiểu int và trả về hiệu của chúng.
    /* // Định nghĩa hàm subtract
    public static int subtract(int a, int b) {
        return a - b;
    }

    public static void main(String[] args) {
        // Gọi hàm subtract và in ra kết quả
        int result = subtract(10, 3);
        System.out.println("Hiệu là: " + result);
    } */ 

// 2. Viết một hàm divide nhận vào hai tham số kiểu double và trả về thương của chúng.
    /*// Định nghĩa hàm divide
    public static double divide(double a, double b) {
        return a / b;
    }

    public static void main(String[] args) {
        // Gọi hàm divide và in ra kết quả
        double result = divide(10.0, 2.0);
        System.out.println("Thương là: " + result);
    }*/ 
}
